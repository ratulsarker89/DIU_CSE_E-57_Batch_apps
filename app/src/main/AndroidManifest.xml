<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ratul_pc.diucse">

    <application
        android:allowBackup="true"
        android:icon="@drawable/diu_logo1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Student" />
        <activity android:name=".Teacher" />
        <activity android:name=".Course_Outline" />
        <activity android:name=".Link" />
        <activity
            android:name=".Tab_Course"
            android:label="@string/title_activity_tab__course"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".News"></activity>
    </application>

</manifest>